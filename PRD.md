# PRD | Resume Formatter v2

## 1. 产品定位

Resume Formatter 是本地优先的简历编辑与排版器，不是求职管理系统。它负责把结构化内容稳定排成可投递的 HTML / PDF，并在用户主动选择时提供受控的 AI 编辑建议。

核心价值：

- 内容与排版分离；
- 用户文件默认不离开浏览器；
- 最终产物仍为一个可离线打开的 HTML；
- AI 只建议，不静默覆盖事实；
- 排版检查可解释，不制造 ATS 通过率。

## 2. 用户与任务

目标用户以中文简历作者为主，同时兼容英文内容。核心任务：

1. 导入已有 Markdown、JSON、JSON Resume、PDF 或 DOCX；
2. 检查栏目映射并修正内容；
3. 选择适合岗位的专业模板；
4. 对选中文字进行专业化、精炼或结果导向改写；
5. 审阅全文问题或与明确提供的 JD 对照；
6. 修复页面溢出并导出独立 HTML / PDF。

## 3. 范围

### 包含

- ResumeDocumentV2 与 Schema v1 自动迁移
- 九类标准栏目和自定义栏目
- Markdown、当前 JSON、JSON Resume、PDF、DOCX 导入
- 25 MB 文件限制和 30 页 PDF 限制
- 六套策展模板、A4 / US Letter、照片和版式令牌
- 桌面与移动端完整编辑
- 六类 BYOK AI 提供商
- 连接测试、选区引用、事实变化保护和原子撤销
- 全文问题清单、JD 对照、确定性关键词命中
- 可解释机器可读性规则
- 本地草稿、版本、HTML / Markdown / JSON / PDF 导出

### 不包含

- OCR、DOCX 导出
- 账号、云同步、团队协作
- 求职看板、模拟面试、自动投递
- 任意 HTML / JavaScript 模板导入
- AI 自动批量覆盖或事实补全

## 4. 核心流程

### 4.1 导入

文件选择 -> 本地解析 -> 栏目检测 -> 映射置信度 / 警告 / 未映射片段 -> 用户确认 -> 规范化文档。

必须满足：

- 扫描 PDF 无可选择文字时提示不支持 OCR；
- 损坏文件返回可恢复错误；
- 未识别文字进入未映射列表，不静默丢弃；
- 导入不会覆盖原文件。

### 4.2 选区 AI

选择文字 -> 就近工具条 -> 选择模式 -> 发送最小上下文 -> 差异预览 -> 事实警告 -> 应用或丢弃 -> 单次撤销。

稳定选区由字段路径、字符起止偏移、原文和 SHA-256 组成。应用前再次验证；原文变化时拒绝覆盖。

数字、日期、比例、金额量级和大写缩写发生增删时，应用按钮必须保持禁用，直到用户显式确认。

### 4.3 全文审阅

用户主动触发后返回逐项问题：严重度、字段路径、标题、问题说明和建议。默认不批量改写。

### 4.4 JD 对照

本地层先计算可解释关键词命中；AI 建议独立展示。岗位要求不能被写成候选人已具备的事实。

### 4.5 导出

- HTML：包含当前 ResumeDocumentV2，剥离所有凭据和临时 UI；
- Markdown / JSON：只导出简历状态；
- PDF：浏览器打印，打印样式隐藏应用控件、空照片占位和溢出标记。

## 5. 模板

| 模板 | 主要用途 | 照片 | 机器读取标签 |
| --- | --- | --- | --- |
| 中文紧凑 | 中文校招 / 社招高密度内容 | 支持 | 标准 |
| 现代无衬线 | 通用产品、设计、技术岗位 | 支持 | 标准 |
| 经典衬线 | 咨询、法律、专业服务 | 不支持 | 标准 |
| 行政简约 | 管理者与高级岗位 | 不支持 | 标准 |
| 学术研究 | 教育、论文、出版物 | 不支持 | 标准 |
| 两栏视觉版 | 视觉岗位与作品展示 | 支持 | 警告 |

每个模板是独立 `TemplateDefinition`，共享受限 `LayoutProfile` 覆盖值。用户不能执行任意 HTML。

## 6. 界面

桌面：52 px 半透明命令栏、左侧文档 / 模板、中央真实纸张、右侧版式 / 检查 / AI。

移动端：内容 / 预览切换、可缩放纸张、可拖动或点击展开的底部检查器。390x844 不保留常驻双侧栏。

所有按钮必须有可访问名称；图标使用 Lucide；键盘焦点清晰；尊重 reduced-motion；圆角不超过 8 px；字距为 0。

## 7. 隐私与联网

无主动 AI 操作时网络请求数为 0。AI 请求只允许由 AI 适配层发起。

凭据默认存入 `sessionStorage`。只有用户勾选“记住到本机”后才写入 `localStorage`，并显示明文风险。凭据不属于简历合同，任何导出均不包含凭据。

供应商接收和保留数据的行为由用户选择的供应商、账号与条款决定。本项目不代理、不审计也不保证第三方数据处理。

## 8. 验收

- 迁移、导入、模板、事实保护和 AI 响应单元测试通过；
- 六类提供商的成功、401、CORS、超时、取消、中断和非法响应有覆盖；
- 1440x1024、1280x800、768x1024、390x844 无视口横向溢出或控件越界；
- PDF / DOCX 可本地解析，扫描 PDF 与损坏 DOCX 返回明确错误；
- 匿名 PDF 为单页 A4 并保留可选择文字；
- 三份单文件产物哈希一致且小于 5 MB；
- 依赖审计、许可证、敏感信息和网络边界检查通过。

## 9. 发布门禁

实施完成后依次经过：路径确认、暂存授权、提交授权、推送授权、Fork main 的 Draft PR。人工审阅后才合并并启用 GitHub Pages；不向上游自动提交 PR。
