# Security

Resume Formatter v2.4 是无后端代理的本地优先浏览器应用。它减少凭据和简历数据的暴露面，但不把浏览器内 BYOK 描述成不可读取的秘密。

## 保护目标

- 简历、岗位、JD、证据、照片和版本默认留在当前浏览器来源；
- API Key 只在内存和当前标签页的 `sessionStorage` 中存在；
- 普通导出不包含岗位工作区资料，任何导出都不包含凭据；
- 未执行明确 AI 命令时不产生网络请求；
- 导入内容不能执行脚本或任意 HTML。

## 已实施控制

- ResumeDocumentV2 通过白名单重建，未知字段和远程照片 URL 被丢弃；
- 导出边界递归删除密钥、密码、凭据、授权和令牌字段，并替换活动密钥文本；
- Gemini 使用 `x-goog-api-key` 请求头，其他供应商使用必要的认证请求头，密钥不进入 URL；
- 内置供应商固定端点；自定义端点禁止 URL 凭据、查询参数和片段，非回环地址必须使用 HTTPS；
- 提供商错误和网络错误统一脱敏；大范围发送前显示目标主机、传输、模型、文本类型和字符数；
- 单文件使用脚本 SHA-256 CSP、`object-src 'none'`、`form-action 'none'`、受限图片 / Worker / 连接来源和 `no-referrer`；
- 清除操作只删除 `resume-formatter:` 前缀的站点数据，不影响同来源其他键。

## 不保护的场景

- 当前页面被恶意修改，或用户打开来源不明的独立 HTML；
- 有权限读取页面、存储或网络请求的浏览器扩展；
- 用户主动打开开发者工具检查内存、`sessionStorage` 或请求头；
- 操作系统、浏览器配置文件或设备已被控制；
- AI 供应商对请求的日志、保留、训练、区域、合规或删除政策；
- 已下载的 HTML、JSON、Markdown 或 PDF 文件。浏览器内清除不会删除这些文件。

因此应使用可撤销、限额和最小权限的 API Key，不在共享设备或不可信页面中使用长期密钥。自定义端点的运营者与所选 AI 供应商属于独立数据处理边界。

## 报告问题

请通过仓库的 GitHub Security Advisory 私下报告可复现的安全问题。报告应使用虚构数据，不要附带真实 API Key、简历、JD、照片或个人联系方式。
