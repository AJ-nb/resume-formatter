# Privacy

Resume Formatter 是本地优先工具。公开页面和离线 HTML 不包含统计、广告、远程字体、第三方脚本或远程图片。页面使用脚本哈希 CSP 和 `Referrer-Policy: no-referrer` 限制运行时能力。

## 默认本地处理

以下数据默认只在浏览器中处理：

- Markdown、JSON、JSON Resume、PDF 和 DOCX 文件；
- 简历正文与布局设置；
- 证件照；
- 本地草稿和版本记录；
- 简历母版、独立岗位版本、完整 JD、事实证据、要求匹配和导出记录；
- PDF / DOCX 提取出的文字和未映射片段。

草稿、版本与工作区可能写入当前来源的 `localStorage`。首次工作区迁移保留旧存储键，并建立迁移前回滚副本。保存的独立 HTML 只内嵌活动简历状态与照片，使其可以离线重新打开；不会内嵌 JD、证据或岗位历史。

## AI 联网条件

只有下列用户命令会发出网络请求：

| 命令 | 发送内容 |
| --- | --- |
| 读取可用模型 | 提供商、Base URL 和凭据；不发送简历正文 |
| 连接测试 | 提供商、Base URL、模型、凭据和最小测试提示 |
| 选区改写 | 选中文字、字段路径、栏目、模式、用户明确提供的 JD 上下文 |
| 证据生成 | 用户选定的证据、目标字段和明确选择的 JD 要求 |
| 全文审阅 | 当前简历的纯文本表示 |
| JD 对照 | 当前简历纯文本和用户粘贴的岗位描述 |

导入、编辑、模板切换、机器读取检查、保存和导出不会主动联网。

全文审阅和 JD 对照会在发送前显示所选提供商、模型、实际目标主机、传输方式、文本类型和字符数。只有用户确认后才调用 AI 适配层；取消确认不会发送文本，进行中的大范围请求也可中止。AI 调用失败后，本地检查、关键词匹配、证据表单和备份仍可使用。

## API Key

API Key 只保存在页面内存和当前标签页的 `sessionStorage`，刷新后仍可使用，关闭标签页后失效。应用不再提供把密钥写入 `localStorage` 的选项；提供商、模型和合法 Base URL 可以作为非敏感偏好保存在本机。

重新打开 AI 设置时，应用只显示“当前标签页已配置”，不会把现有密钥写回输入框。“忘记 API Key”会立即清除内存、当前会话凭据、连接状态和旧版本遗留的本机明文凭据。

浏览器直连意味着当前页面环境可以读取密钥。不要在以下环境使用长期密钥：

- 共享或受管理的设备；
- 来源不明或被他人修改的 HTML；
- 安装了不可信扩展的浏览器；
- 无法确认供应商端点的自定义 Base URL。

建议使用可撤销、限额和最小权限的密钥。切换提供商会清空设置表单中的密钥；更换提供商、Base URL、模型或密钥后必须重新连接测试。错误信息会删除当前密钥、Bearer 凭据、常见密钥格式和带凭据 URL，但这不能防止开发者工具或扩展直接检查页面和网络请求。

内置提供商固定官方端点。代理或聚合服务必须选择“自定义兼容端点”；自定义 URL 不得包含用户名、密码、查询参数或片段，必须使用 HTTPS，只有 `localhost`、`127.0.0.1` 和输入后规范化为 `localhost` 的 `[::1]` 可以使用 HTTP。Gemini 密钥只通过 `x-goog-api-key` 请求头发送，不进入 URL。

## 导出

凭据不属于 ResumeDocumentV2 或 ApplicationWorkspaceV1。HTML、Markdown、简历 JSON、工作区 JSON 和 PDF 导出会排除 API Key 与 AI 设置。JSON、HTML 和工作区导出还会在边界处递归剥离密钥、密码、凭据和访问令牌字段，并替换当前活动密钥文本。自动化测试会检查这一边界。

普通简历导出不包含 JD、证据或岗位历史。工作区 JSON 会包含这些本地敏感资料与照片资产，导出前会明确提示范围；它用于个人备份，不适合作为投递附件。

岗位版本在用户通过门禁并打开投递打印流程后会在本地标记为只读快照。复制继续会建立新的草稿记录，不修改原投递快照。

## 供应商边界

当用户主动调用 AI 时，数据直接发送到所选提供商：OpenAI、DeepSeek、Gemini、OpenRouter、彼源 AI、Ollama 或自定义 OpenAI-compatible 端点。

彼源 AI 的“读取可用模型”会使用用户填写的令牌请求 `https://api.biyuan.ai/v1/models`。这一步不包含简历正文；后续连接测试和 AI 命令的数据处理边界仍由彼源 AI 的账号、分组、模型与服务条款决定。

本项目不代理请求，也无法控制或验证第三方的日志、保留、训练、区域、合规和删除政策。用户应根据自己的账号计划、组织政策和供应商条款判断是否可以发送简历或岗位信息。

Ollama 默认指向本机地址，但是否只在本机处理取决于用户自己的 Ollama 配置和网络环境。

## 清除本地数据

“隐私与本地数据”面板会列出简历、岗位、照片、版本、JD 字符、证据和当前标签页凭据状态，并先提供工作区备份入口。“清除全部本地数据”只删除当前来源下以 `resume-formatter:` 开头的 `localStorage` 和 `sessionStorage` 键，不调用全局 `storage.clear()`，不会影响同来源的其他应用数据。清除后会建立一个空白工作区并清空撤销栈和 AI 结果。

独立保存的 HTML、简历 JSON、工作区 JSON、Markdown 和 PDF 是普通本地文件，浏览器无法替用户删除。独立 HTML 中内嵌的活动简历也不会因清除浏览器站点数据而消失，用户必须自行删除或替换下载文件。
