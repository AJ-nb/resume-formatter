# Privacy

Resume Formatter 是本地优先工具。公开页面和离线 HTML 不包含统计、广告或第三方运行时资源。

## 默认本地处理

以下数据默认只在浏览器中处理：

- Markdown、JSON、JSON Resume、PDF 和 DOCX 文件；
- 简历正文与布局设置；
- 证件照；
- 本地草稿和版本记录；
- PDF / DOCX 提取出的文字和未映射片段。

草稿与版本可能写入当前来源的 `localStorage`。保存的独立 HTML 会内嵌简历状态与照片，使其可以离线重新打开。

## AI 联网条件

只有下列用户命令会发出网络请求：

| 命令 | 发送内容 |
| --- | --- |
| 连接测试 | 提供商、Base URL、模型、凭据和最小测试提示 |
| 选区改写 | 选中文字、字段路径、栏目、模式、用户明确提供的 JD 上下文 |
| 全文审阅 | 当前简历的纯文本表示 |
| JD 对照 | 当前简历纯文本和用户粘贴的岗位描述 |

导入、编辑、模板切换、机器读取检查、保存和导出不会主动联网。

## API Key

API Key 默认保存在 `sessionStorage`，关闭标签页后失效。只有用户勾选“记住到本机”后，凭据才以明文写入 `localStorage`。

浏览器直连意味着当前页面环境可以读取密钥。不要在以下环境使用长期密钥：

- 共享或受管理的设备；
- 来源不明或被他人修改的 HTML；
- 安装了不可信扩展的浏览器；
- 无法确认供应商端点的自定义 Base URL。

建议使用可撤销、限额和最小权限的密钥。更换提供商、Base URL、模型或密钥后必须重新连接测试。

## 导出

凭据不属于 ResumeDocumentV2。HTML、Markdown、JSON 和 PDF 导出会排除 API Key 与 AI 设置。自动化测试会检查这一边界。

## 供应商边界

当用户主动调用 AI 时，数据直接发送到所选提供商：OpenAI、DeepSeek、Gemini、OpenRouter、Ollama 或自定义 OpenAI-compatible 端点。

本项目不代理请求，也无法控制或验证第三方的日志、保留、训练、区域、合规和删除政策。用户应根据自己的账号计划、组织政策和供应商条款判断是否可以发送简历或岗位信息。

Ollama 默认指向本机地址，但是否只在本机处理取决于用户自己的 Ollama 配置和网络环境。

## 清除本地数据

可以通过浏览器站点数据设置清除 `localStorage` 和 `sessionStorage`。独立保存的 HTML、JSON、Markdown 和 PDF 是普通本地文件，需要由用户自行删除。
