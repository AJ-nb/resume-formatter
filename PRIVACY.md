# Privacy

Resume Formatter 是本地优先工具。公开页面和离线 HTML 不包含统计、广告或第三方运行时资源。

## 默认本地处理

以下数据默认只在浏览器中处理：

- Markdown、JSON、JSON Resume、PDF 和 DOCX 文件；
- 简历正文与布局设置；
- 证件照；
- 本地草稿和版本记录；
- 简历母版、独立岗位版本、完整 JD、事实证据、要求匹配和导出记录；
- PDF / DOCX 提取出的文字和未映射片段。

草稿、版本与工作区可能写入当前来源的 `localStorage`。首次工作区迁移保留旧存储键，并建立迁移前回滚副本。保存的独立 HTML 只内嵌活动简历状态与照片，使其可以离线重新打开；不会内嵌 JD、证据或岗位历史。

## AI 联网条件

只有下列用户命令会发出网络请求：

| 命令 | 发送内容 |
| --- | --- |
| 读取可用模型 | 提供商、Base URL 和凭据；不发送简历正文 |
| 连接测试 | 提供商、Base URL、模型、凭据和最小测试提示 |
| 选区改写 | 选中文字、字段路径、栏目、模式、用户明确提供的 JD 上下文 |
| 证据生成 | 用户选定的证据、目标字段和明确选择的 JD 要求 |
| 全文审阅 | 当前简历的纯文本表示 |
| JD 对照 | 当前简历纯文本和用户粘贴的岗位描述 |

导入、编辑、模板切换、机器读取检查、保存和导出不会主动联网。

## API Key

API Key 默认保存在 `sessionStorage`，关闭标签页后失效。只有用户勾选“记住到本机”后，凭据才以明文写入 `localStorage`。

浏览器直连意味着当前页面环境可以读取密钥。不要在以下环境使用长期密钥：

- 共享或受管理的设备；
- 来源不明或被他人修改的 HTML；
- 安装了不可信扩展的浏览器；
- 无法确认供应商端点的自定义 Base URL。

建议使用可撤销、限额和最小权限的密钥。切换提供商会清空设置表单中的密钥；更换提供商、Base URL、模型或密钥后必须重新连接测试。

## 导出

凭据不属于 ResumeDocumentV2 或 ApplicationWorkspaceV1。HTML、Markdown、简历 JSON、工作区 JSON 和 PDF 导出会排除 API Key 与 AI 设置。自动化测试会检查这一边界。

普通简历导出不包含 JD、证据或岗位历史。工作区 JSON 会包含这些本地敏感资料与照片资产，导出前会明确提示范围；它用于个人备份，不适合作为投递附件。

## 供应商边界

当用户主动调用 AI 时，数据直接发送到所选提供商：OpenAI、DeepSeek、Gemini、OpenRouter、彼源 AI、Ollama 或自定义 OpenAI-compatible 端点。

彼源 AI 的“读取可用模型”会使用用户填写的令牌请求 `https://api.biyuan.ai/v1/models`。这一步不包含简历正文；后续连接测试和 AI 命令的数据处理边界仍由彼源 AI 的账号、分组、模型与服务条款决定。

本项目不代理请求，也无法控制或验证第三方的日志、保留、训练、区域、合规和删除政策。用户应根据自己的账号计划、组织政策和供应商条款判断是否可以发送简历或岗位信息。

Ollama 默认指向本机地址，但是否只在本机处理取决于用户自己的 Ollama 配置和网络环境。

## 清除本地数据

可以通过浏览器站点数据设置清除 `localStorage` 和 `sessionStorage`。独立保存的 HTML、简历 JSON、工作区 JSON、Markdown 和 PDF 是普通本地文件，需要由用户自行删除。
